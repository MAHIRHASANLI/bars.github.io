import PartnesrContainer from "@/container/partners";
import DefaultLayout from "@/default_layout";
import React from "react";

type Props = {};

const PartnersPage = (props: Props) => {
  return (
    <DefaultLayout>
      <PartnesrContainer />
    </DefaultLayout>
  );
};

export default PartnersPage;
