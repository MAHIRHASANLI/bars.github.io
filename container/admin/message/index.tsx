import MessageSection from "@/sections/admin/message";
import React from "react";

type Props = {};

const MessageConteiner = (props: Props) => {
  return <MessageSection />;
};

export default MessageConteiner;
