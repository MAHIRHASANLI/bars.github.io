import ProductsListComponent from "@/components/admin/product_list";
import React from "react";

type Props = {};

const ProductDashboard = (props: Props) => {
  return <ProductsListComponent />;
};

export default ProductDashboard;
